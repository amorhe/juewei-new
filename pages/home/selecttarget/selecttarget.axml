<!-- <view>
  商城引导定位页面
  <view>{{logtxt}}</view>
</view> -->

<view class="searchView">
  <view>
    <view class="positionAddress" onTap="choosecityTap">
      <text>{{city}}</text>
      <image src="{{imageUrl}}arrow_down1.png" />
    </view>
    <view class="searchBox">
      <image class="searchIcon" src="{{imageUrl}}search.png" />
      <input type="text" placeholder="小区/写字楼/学校等" placeholder-class="colorGray"  />
    </view>
  </view>
</view> 

<view class="positionIng">
  <view class="fontGray">当前定位</view>
  <view class="positionTxt">
    <view onTap="switchAddress" data-info="{{addressIng}}">
      <image src="{{imageUrl}}position.png"/>
      <text>{{addressIng}}</text>
    </view>
    <view class="numberColor fontTwentySix" onTap="rePosition">重新定位</view>
  </view>
</view>

<!-- 收货地址 -->
<view class="delivery">
  <view class="fontGray">收货地址</view>
  <view class="addressList">
    <view a:for="{{canUseAddress}}" a:if="{{canUseAddress.length>0}}">
      <view class="addressTxt">
        <view class="fontBold">{{item.user_address_address}}</view>
        <view a:if="{{item.tag!=0}}" class="{{item.tag==1?'addressTabbar bar1':item.tag==2?'addressTabbar bar2':'addressTabbar bar3'}}">{{item.tag==1?'家':item.tag==2?'公司':'学校'}}</view>
      </view>
      <view class="fontGray">{{item.user_address_name}}（{{item.user_address_sex==0?'先生':'女士'}}） {{item.user_address_phone}}</view>
    </view>
    <!-- <view>
      <view class="addressTxt">
        <view class="fontBold">紫檀大厦 606</view>
        <view class="addressTabbar bar2">公司</view>
      </view>
      <view class="fontGray">小春（先生） 188 8888 8888</view>
    </view>

    <view>
      <view class="addressTxt">
        <view class="fontBold">紫檀大厦 606</view>
        <view class="addressTabbar bar3">家</view>
      </view>
      <view class="fontGray">小春（先生） 188 8888 8888</view>
    </view> -->
  </view>
  <navigator url="/package_my/pages/myaddress/addaddress/addaddress" class="addAddressTxt">
    <text>新增地址</text>
    <image class="normalImage" src="{{imageUrl}}arrow_right1.png"/>
  </navigator>
</view>

<!-- 附近地址 -->
<view class="nearAddressBox">
  <view class="fontGray">附近地址</view>
  <view class="nearList" a:for="{{nearAddress}}" onTap="switchAddress" data-info="{{item}}">{{item.name}}</view>
  <!-- <view class="nearList">北京传媒大学西校区</view> -->
</view>